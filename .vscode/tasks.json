{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-tests",
      "type": "shell",
      "command": "mkdir -p build && g++ -std=c++20 -g tests/checks_tests.cpp Card.cpp Checks/*.cpp -o build/checks_tests",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": false
      }
    },
    {
      "label": "build-main",
      "type": "shell",
      "command": "mkdir -p build && g++ -std=c++20 -g main.cpp Card.cpp Deck.cpp Checks/*.cpp -o build/main",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
